# Repo-local environment variables (not committed).
# Copy this file to `.env` and fill in secrets as needed.
# Devcontainer + docker-compose read values from this file; for local backend runs without a devcontainer, use `backend/.env`.
#
# GitHub fine-grained PAT scoped to this repo (used by Codex for `gh` and HTTPS `git push`).
# Required scopes typically include: Contents (read/write), Pull requests (read/write), Issues (read).
# Prefer the repo-specific name; GH_AUTH_TOKEN/GH_TOKEN/GITHUB_TOKEN are also accepted.
CALIBRATE_GH_PAT=
GITHUB_TOKEN=

# Optional: override the host Codex credentials directory mounted into the devcontainer.
# Defaults to ~/.codex when unset.
CODEX_HOME=

# Food data providers (used by devcontainer + docker-compose).
FATSECRET_CLIENT_ID=
FATSECRET_CLIENT_SECRET=

USDA_API_KEY=

# Local root scripts (`npm run dev` and `npm run dev:test`) set `VITE_ENABLE_SW_DEV=1` automatically.
# If you run the frontend directly and need local PWA/push behavior, set this flag for that process.
# VITE_ENABLE_SW_DEV=1

# Web push (VAPID) configuration used by backend push endpoints.
# Devcontainer initialization auto-generates these when missing and writes them to `.devcontainer/.env`.
# Set these in repo `.env` to override generated values, or when using local/docker workflows outside the devcontainer.
WEB_PUSH_PUBLIC_KEY=
WEB_PUSH_PRIVATE_KEY=
WEB_PUSH_SUBJECT=mailto:you@example.com
