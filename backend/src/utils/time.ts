export const MS_PER_MINUTE = 60 * 1000; // Milliseconds in one minute.
const MS_PER_HOUR = 60 * MS_PER_MINUTE; // Milliseconds in one hour.
export const MS_PER_DAY = 24 * MS_PER_HOUR; // Milliseconds in one day.
