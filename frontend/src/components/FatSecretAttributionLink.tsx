import React from 'react';

/**
 * Render the exact FatSecret attribution snippet (HTML must remain unchanged for compliance).
 */
const FatSecretAttributionLink: React.FC = () => {
    return <a href="https://www.fatsecret.com">Powered by fatsecret</a>;
};

export default FatSecretAttributionLink;
