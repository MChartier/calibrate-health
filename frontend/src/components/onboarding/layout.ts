/**
 * Controls vertical spacing within the onboarding card while keeping the UI compact on iPhone SE-class screens.
 */
export const ONBOARDING_CARD_CONTENT_SPACING = 1.75;

/**
 * Controls spacing between form fields within a step (used heavily in the guided "reveal" flow).
 */
export const ONBOARDING_FIELD_SPACING = 1.25;

/**
 * Controls spacing within the fixed footer area (prompt + back/next buttons).
 */
export const ONBOARDING_FOOTER_SPACING = 1;
