import React from 'react';
import TodayWorkspace from '../components/TodayWorkspace';

/**
 * Dashboard route now hosts the Today workspace, the primary signed-in experience.
 */
const Dashboard: React.FC = () => <TodayWorkspace />;

export default Dashboard;
